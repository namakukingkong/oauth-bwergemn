# frozen_string_literal: true

RSpec.describe OauthBwergemn do
  it 'has a version number' do
    expect(OauthBwergemn::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
    expect(true).to eq(true)
  end
end
